
############################
# Credential Configuration #
############################
# Note: you can specify two different configurations if you use a
# master ldap for writing access and a slave ldap server for reading access
# By default, we will use the same DN (so it will work for standard Samba
# release)
slaveDN="cn=Manager,dc=ldap,dc=home,dc=prettybaked,dc=com"
slavePw="throve_shutoff_stubb_anon_invert"
masterDN="cn=Manager,dc=ldap,dc=home,dc=prettybaked,dc=com"
masterPw="throve_shutoff_stubb_anon_invert"
